/**
 * Comptage du nombre d'occurrences d'un caractère
 *
 * Compte le nombre de fois qu'un caractère donné apparaît dans une chaîne.
 *
 * Pseudo-code :
 * Initialiser un compteur à 0
 * Pour chaque caractère de la chaîne
 *    si le caractère est égal au caractère recherché
 *       incrémenter le compteur
 *    fin si
 * Fin Pour
 * Retourner le compteur
 *
 * @param {string} str - La chaîne
 * @param {string} char - Le caractère à compter
 * @returns {number} - Le nombre d'occurrences du caractère
 */

export default function charCount(str, char) {
  // Votre code ici
}
