/**
 * Parcours en largeur (BFS)
 *
 * Ce parcours visite tous les nœuds d'une profondeur avant de passer à la suivante.
 *
 * Pseudo-code :
 * fonction BFS(noeud)
 *    initialiser une file
 *    ajouter le nœud de départ à la file
 *    tant que la file n'est pas vide
 *       noeud = file.retirer()
 *       pour chaque voisin de noeud
 *          si voisin n'est pas visité
 *             marquer voisin comme visité
 *             ajouter voisin à la file
 *          fin si
 *       fin pour
 *    fin tant que
 * fin fonction
 *
 * @param {Node} node - Le nœud de départ
 */

export default function breadthFirstSearch(node) {
  // Votre code ici
}
