/**
 * Génération de combinaisons et de permutations
 *
 * Générer toutes les combinaisons ou permutations possibles d'une chaîne de caractères ou d'un tableau.
 *
 * Pseudo-code :
 * fonction generateCombPerms(input, prefix = "", result = [])
 *    si l'input est vide
 *       ajouter prefix aux résultats
 *       retourner
 *    fin si
 *    pour chaque caractère dans input
 *       appeler generateCombPerms avec le reste de l'input et le préfixe actuel + caractère actuel
 *    fin pour
 * fin fonction
 *
 * @param {string} input - La chaîne de caractères ou le tableau
 * @returns {string[]} - Les combinaisons/permutations possibles
 */

export default function generateCombinations(input) {
  // Votre code ici
}
