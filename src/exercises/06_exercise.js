/**
 * Recherche linéaire
 *
 * Parcourt l'ensemble du tableau élément par élément jusqu'à ce que l'élément
 * recherché soit trouvé ou que le tableau entier soit parcouru.
 *
 * Pseudo-code :
 * Pour chaque élément du tableau
 *    si l'élément est égal à la valeur recherchée
 *       retourner l'index de l'élément
 *    fin si
 * Fin Pour
 * retourner -1
 *
 * @param {number[]} arr - Le tableau
 * @param {number} val - La valeur à rechercher
 * @returns {number} - L'index de la valeur ou -1 si non trouvée
 */

export default function linearSearch(arr, val) {
  // Votre code ici
}
