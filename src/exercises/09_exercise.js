/**
 * Trouver le premier caractère non répété
 *
 * Parcourt la chaîne et trouve le premier caractère qui n'apparaît qu'une seule fois.
 *
 * Pseudo-code :
 * Pour chaque caractère de la chaîne
 *    si le nombre d'occurrences du caractère est 1
 *       retourner le caractère
 *    fin si
 * Fin Pour
 * Retourner null
 *
 * @param {string} str - La chaîne
 * @returns {string|null} - Le premier caractère non répété ou null si tous sont répétés
 */
export default function firstNonRepeatingChar(arr) {
  // Votre code ici
}
